# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|

## User Preferences
- Single-file HTML project (~6500 lines) — no build tools, no frameworks
- Conventional commits (feat:, fix:, refactor:, etc.)

## Patterns That Work
- (approaches that succeeded)

## Patterns That Don't Work
- (approaches that failed and why)

## Domain Notes
- Classplay Design System: self-contained docs site in `index.html`
- CSS tokens use `--cp-` prefix, layout classes use `ds-` prefix, component classes use `cp-` prefix
- Theme system: `data-theme` attribute on `<html>`, persisted to localStorage
